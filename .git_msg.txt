fix: start slideshow before sharing screen to prevent window freeze for participants

PowerPoint slideshow starts in a separate window ("PowerPoint Slide Show"). Previously, the controller shared the window while it was in edit mode, then started the slideshow, leaving participants looking at the static editing view while the bot controlled the active slideshow window locally. Moved slideshow start (`start_immediately=True`) before the sharing flow so that the active slideshow window is shared directly.

perf: optimize AudioController routing, caching, and stream callbacks

Backend:
- app/modules/meeting_bot/media/audio_controller.py:
  - Cache target device index at initialization/validation to avoid polling sound devices on every play
  - Use case-insensitive and prefix check for flexible device name matching ("CABLE Input" / "CABLE Input (VB-Audio...)")
  - Replace sd.play convenience wrapper with explicit low-latency sd.OutputStream callback mechanism
  - Add explicit sample rate, channel mapping validation, and mono-to-stereo normalization before starting streams
  - Enhance log metrics to dump Device Index, Sample Rate, Channels, and Duration on playback start

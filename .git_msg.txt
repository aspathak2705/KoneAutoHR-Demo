fix: bypass script and FAQ requirement in runtime preloading for HR Recorded sessions

- backend/app/services/runtime_service.py (get_runtime_context): Bypass validation errors for missing AI script and employee FAQs if creation_mode is HR. Synthesize a mock script structure from the slide count to avoid downstream Null Pointer Exceptions in UI/Timeline mapping, and safely assign script ID.

fix: resolve presentation path mapping in presentation runtime controller

In HR mode, packages are stored in a structured subfolder `presentation/` inside the session directory. Modified `presentation_runtime_controller.py` to check for the file at `session_dir / "presentation" / presentation_name` if it is not present directly in `session_dir / presentation_name`, resolving the COM dispatch open failures.

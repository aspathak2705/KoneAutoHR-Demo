feat: add startup diagnostics and verification to AudioController

Backend:
- app/modules/meeting_bot/media/audio_controller.py:
  - Enumerate and log all detected output devices on startup for diagnostic visibility
  - Validate that the matched device supports output channels (max_output_channels > 0)
  - Verify output device accessibility at validation stage using sd.check_output_settings to fail fast before playback begins
